export enum CompositekeyFilter {
  BEGINS_WITH = "beginsWith",
  CONTAINS = "contains",
  EQUAL = "eq",
}

export enum DBEntityType {
  USER = "USER",
  STATS = "STATS",
}

export enum UserType {
  MANUFACTURER,
  RECYCLER,
}
