export enum InjectionToken {
  APPLICATION_CONFIG = "TOKEN_APPLICATION_CONFIG",
}

export enum StoreModelEntryKey {
  USER,
}

export enum ConfigEntries {
  SYSTEM = "system",
  AUTH = "auth",
}

export enum AuthScope {
  READ,
  WRITE_MANUFACTURER,
  WRITE_RECYCLER,
}

export enum AsyncStorageEntries {
  JWT_PAYLOAD,
}
